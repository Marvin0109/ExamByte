version: '3.8'
services:
  exambyteDB:
    image: postgres:15-alpine
    container_name: exambyte_postgres
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql/data
    environment:
      - "POSTGRES_DB=exambyte_db"
      - "POSTGRES_USER=exambyte_user"
      - "POSTGRES_PASSWORD=exambyte_password"